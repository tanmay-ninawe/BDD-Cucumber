# BDD-Cucumber

// to run the project in VS code - 
 - make sure you've all the Extensions added/Installed
 - with Dependencies (Selenium / TestNG / Cucumber / Cucumber TestNG[*for running the TestRunner file*])
 - To run the project make sure you're in folder of Pom.xml
 - and from there hot the command mvn test. 